Testumgebung:

Laden der Testumgebung mit c(testumgebung)
[Ggf. noch zusätzlich c(util)]

Aufruf mit
testumgebung:messen(AnzahlElemente).


Da die Erstellung der Listen deutlich länger braucht als die Ausführung der Operationen, wird nach Erstellung der Listen ausgegeben, dass jetzt die eigentliche Messung beginnt.

Rückgabe:
Listen erstellt
Laufzeit insertBT: 16631 Mikrosekunden
Laufzeit deleteBT: 2782 Mikrosekunden


